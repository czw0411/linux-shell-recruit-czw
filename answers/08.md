# Task 08 Answer
`$var` 不加引号时，Shell 会对展开结果做"分词"（word splitting）和通配符展开：如果变量的值里含空格，如 `My Report.txt`，会被当成两个词 `My` 和 `Report.txt`。

`"$var"` 加引号后，整个值被视为一个整体，空格不再拆分，因此含空格的文件名能作为一个参数正确传递，命令 `cp` 收到的就是完整的文件名。
